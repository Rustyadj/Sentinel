// Backward-compatible alias. The SDK-backed integration OAuth stack is the
// only implementation; stale clients can keep their registered URL.
export { POST } from "@/app/api/integrations/oauth/register/route";
