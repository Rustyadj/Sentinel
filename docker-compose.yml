services:
  postgres:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_USER: hermes
      POSTGRES_PASSWORD: hermes
      POSTGRES_DB: hermesos
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - sentinel_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hermes -d hermesos"]
      interval: 5s
      timeout: 5s
      retries: 10

  migrate:
    build:
      context: .
      target: builder
      args:
        SENTINEL_COMMIT: ${SENTINEL_RELEASE_SHA:-unknown}
        SENTINEL_BUILT_AT: ${SENTINEL_BUILT_AT:-unknown}
    command: npx prisma@6 migrate deploy
    environment:
      DATABASE_URL: postgresql://hermes:hermes@postgres:5432/hermesos
    networks:
      - sentinel_net
    depends_on:
      postgres:
        condition: service_healthy
    restart: on-failure

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    # noeviction, not allkeys-lru: this Redis now backs the Learning Core
    # BullMQ queue (src/lib/learning/queue.ts), and LRU eviction can
    # silently drop queued/delayed job data under memory pressure — BullMQ
    # itself warns about this at connection time. Confirmed by actually
    # connecting a real Queue/Worker against this policy in this session
    # (see the PR — this isn't a guess). Nothing else using this instance
    # (src/lib/redis.ts's health-check client) depends on eviction behavior.
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy noeviction
    volumes:
      - redis_data:/data
    networks:
      - sentinel_net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  app:
    build:
      context: .
      target: runner
      args:
        SENTINEL_COMMIT: ${SENTINEL_RELEASE_SHA:-unknown}
        SENTINEL_BUILT_AT: ${SENTINEL_BUILT_AT:-unknown}
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://hermes:hermes@postgres:5432/hermesos
      REDIS_URL: redis://redis:6379
      AUTH_SECRET: ${AUTH_SECRET}
      AUTH_URL: ${AUTH_URL:-https://sentinel.srv1427612.hstgr.cloud}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      GITHUB_CLIENT_ID: ${GITHUB_CLIENT_ID:-}
      GITHUB_CLIENT_SECRET: ${GITHUB_CLIENT_SECRET:-}
      SENTINEL_DEFAULT_UI: ${SENTINEL_DEFAULT_UI:-true}
      SENTINEL_RELEASE_SHA: ${SENTINEL_RELEASE_SHA:-unknown}
      SENTINEL_COMMIT: ${SENTINEL_RELEASE_SHA:-unknown}
      SENTINEL_BUILT_AT: ${SENTINEL_BUILT_AT:-unknown}
      SENTINEL_TELEMETRY_URL: ${SENTINEL_TELEMETRY_URL:-}
      SENTINEL_TELEMETRY_TOKEN: ${SENTINEL_TELEMETRY_TOKEN:-}
      HERMES_ENDPOINT: ${HERMES_ENDPOINT:-http://host.docker.internal:4860}
      HERMES_CLINT_ENDPOINT: ${HERMES_CLINT_ENDPOINT:-http://host.docker.internal:4861}
      HERMES_CLINT_ENABLED: ${HERMES_CLINT_ENABLED:-false}
      OPENCLAW_ENDPOINT: ${OPENCLAW_ENDPOINT:-http://host.docker.internal:3001}
      HERMES_LISA_CONTAINER: ${HERMES_LISA_CONTAINER:-hermes-lisa}
      HERMES_CLINT_CONTAINER: ${HERMES_CLINT_CONTAINER:-hermes-clint}
      OPENCLAW_CONTAINER: ${OPENCLAW_CONTAINER:-openclaw}
      HERMES_LISA_NATIVE_URL: ${HERMES_LISA_NATIVE_URL:-/legacy/hermes}
      HERMES_CLINT_NATIVE_URL: ${HERMES_CLINT_NATIVE_URL:-}
      OPENCLAW_NATIVE_URL: ${OPENCLAW_NATIVE_URL:-/legacy/openclaw}
      HERMES_LISA_MODEL: ${HERMES_LISA_MODEL:-claude-sonnet-4-6}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      ALLOW_BROWSER_PROVIDER_KEYS: "false"
      HERMES_CLINT_MODEL: ${HERMES_CLINT_MODEL:-claude-sonnet-4-6}
      OPENCLAW_MODEL: ${OPENCLAW_MODEL:-claude-opus-4-8}
      LIVEKIT_URL: ${LIVEKIT_URL:-}
      LIVEKIT_API_KEY: ${LIVEKIT_API_KEY:-}
      LIVEKIT_API_SECRET: ${LIVEKIT_API_SECRET:-}
      VOICE_WORKER_SECRET: ${VOICE_WORKER_SECRET:-}
      AGENT_CONFIG_DIR: /opt/sentinel-os/agents
      AGENT_LOG_DIR: /opt/sentinel-os/logs
      AGENT_PROJECT_ROOT: /opt/sentinel-os/projects
      CLAUDE_CODE_EXECUTABLE: ${CLAUDE_CODE_EXECUTABLE:-claude}
      CLAUDE_CODE_PROJECT_ROOT: /opt/sentinel-os/projects
      CLAUDE_CODE_WORKSPACE_ID: ${CLAUDE_CODE_WORKSPACE_ID:-}
      CODEX_EXECUTABLE: ${CODEX_EXECUTABLE:-codex}
      CODEX_PROJECT_ROOT: /opt/sentinel-os/projects
      CODEX_WORKSPACE_ID: ${CODEX_WORKSPACE_ID:-}
    volumes:
      - agent_configs:/opt/sentinel-os/agents
      - agent_logs:/opt/sentinel-os/logs
      - ${AGENT_PROJECT_ROOT_HOST:-./runtime-projects}:/opt/sentinel-os/projects
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - sentinel_net
      - openclaw-cash_default
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3000/api/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    # Traefik routing is managed via /etc/traefik/dynamic/sentinel.yml on the host

  # Learning Core worker — BullMQ consumer for the recurring jobs registered
  # in src/lib/learning/queue.ts (degradation sweep, knowledge-gap analysis,
  # experience replay). Runs from the `builder` stage (needs devDependencies
  # for tsx + full source, same pattern as `migrate`) rather than a separate
  # compiled image — simplest correct thing until this needs its own
  # deployment lifecycle. Verified live: connection, job execution, retry +
  # dead-letter, scheduler dedup across restarts. `command` invokes the tsx
  # binary directly rather than `npm run worker:learning` — verified that
  # `npm run` does not forward SIGTERM to the underlying process, which
  # would make `docker compose stop`/`down` hard-kill in-flight jobs instead
  # of shutting the worker down gracefully.
  learning-worker:
    build:
      context: .
      target: builder
    command: node_modules/.bin/tsx src/workers/learning-worker.ts
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://hermes:hermes@postgres:5432/hermesos
      REDIS_URL: redis://redis:6379
      LEARNING_WORKER_CONCURRENCY: ${LEARNING_WORKER_CONCURRENCY:-2}
      NEURAL_SWEEP_SECRET: ${NEURAL_SWEEP_SECRET:-}
    networks:
      - sentinel_net
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  # SentinelVoiceGateway worker — LiveKit Agents (Silero VAD + turn detector +
  # Deepgram Flux STT + Cartesia Sonic 3.5 TTS), routed through this app's own
  # /api/chat. Behind a profile since it needs LiveKit/Deepgram/Cartesia
  # credentials most deployments won't have configured:
  #   docker compose --profile voice up
  voice-worker:
    profiles: ["voice"]
    build:
      context: ./worker
    restart: unless-stopped
    environment:
      LIVEKIT_URL: ${LIVEKIT_URL:-}
      LIVEKIT_API_KEY: ${LIVEKIT_API_KEY:-}
      LIVEKIT_API_SECRET: ${LIVEKIT_API_SECRET:-}
      DEEPGRAM_API_KEY: ${DEEPGRAM_API_KEY:-}
      CARTESIA_API_KEY: ${CARTESIA_API_KEY:-}
      SENTINEL_API_BASE_URL: ${SENTINEL_API_BASE_URL:-http://app:3000/api/chat}
      VOICE_WORKER_SECRET: ${VOICE_WORKER_SECRET:-}
    networks:
      - sentinel_net
    depends_on:
      app:
        condition: service_healthy

networks:
  sentinel_net:
    driver: bridge
  # openclaw-cash's own compose-managed network. Reaching it via
  # host.docker.internal:50348 times out for container->container traffic
  # (NAT hairpin through the host gateway to a docker-proxy'd port on the
  # default bridge is unreliable) even though the port answers instantly
  # from the host and from hermes-lisa's host-bound process. Joining this
  # network directly and addressing the container by its compose alias
  # ("cash") sidesteps that path entirely. External: true because it's
  # owned and created by the openclaw-cash compose project, not this one.
  openclaw-cash_default:
    external: true

volumes:
  postgres_data:
  redis_data:
  agent_configs:
  agent_logs:
